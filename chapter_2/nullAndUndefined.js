
let age;

// Note: undefined are variables that were declarated but
// not been assigned any value.
console.log(age, age + 3, `the age is ${age}`)

// check the difference here.
age = null;
console.log(age, age + 3, `the age is ${age}`)